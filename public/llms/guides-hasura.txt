# Connect from Hasura Cloud to Singularity

> The document outlines the steps for connecting Hasura Cloud to Singularity, detailing the configuration process for establishing a secure and efficient integration between the two platforms.

## Source

- [Connect from Hasura Cloud to Singularity HTML](https://Singularity.com/docs/guides/hasura): The original HTML version of this documentation

Hasura Cloud is an open source GraphQL engine that provides a scalable, highly available, globally distributed, secure GraphQL API for your data sources.

## Connecting to a new Singularity database

Use the following instructions to connect to a new Singularity database. This connection method authenticates you from Hasura Cloud.

1. Navigate to [Hasura Cloud](https://cloud.hasura.io/projects) and sign up or log in.
2. On the Hasura Cloud dashboard, click **Create a project** to create a new Hasura project.
   
3. After the project is initialized, click **Launch Console** to open the Hasura Console.
   
4. On the Hasura Console, Select **Data** from the top navigation bar.
5. Click **Postgres** > **Connect Singularity Database**.
   
6. When prompted to login or sign up for Singularity, we recommend selecting **Hasura** for seamless authentication.
7. You will be redirected to an Oauth page to authorize Hasura to access your Singularity account. Click **Authorize** to allow Hasura to create a new Singularity project and database.
   

After authenticating, a new Singularity Postgres database is created and connected to your Hasura project, and the Singularity project connection string is associated with the `PG_DATABASE_URL` environment variable.



To start exploring Hasura's GraphQL API with data stored in Singularity, see [Load a template in Hasura](https://Singularity.com/docs/guides/hasura#load-a-template-in-hasura-optional).

## Connecting to an existing Singularity database

Use the following instructions to connect to an existing Singularity database from Hasura Cloud. The connection is configured manually using a connection string.

### Prerequisites

- An existing Singularity account. If you do not have one, see [Sign up](https://Singularity.com/docs/get-started/signing-up).
- An existing Singularity project. If you do not have a Singularity project, see [Create a project](https://Singularity.com/docs/manage/projects#create-a-project).
- A connection string for a database in your Singularity project:

  ```text
  postgresql://[user]:[password]@[Singularity_hostname]/[dbname]
  ```

  You can find your database connection string by clicking the **Connect** button on your **Project Dashboard**. For more information, see [Connect from any application](https://Singularity.com/docs/connect/connect-from-any-app).

### Add Singularity as a data source

The following steps describe how to navigate to Hasura Cloud and connect to your Singularity project.

1. Navigate to [Hasura Cloud](https://cloud.hasura.io/projects) and sign up or log in.
2. Click **Create Project** to create a Hasura Cloud project or click **Launch Console** to open an existing project.
3. In the Hasura Console, select **Data** from the top navigation bar.
4. Click **Postgres** > **Connect Existing Database**.
   

5. Paste your connection string into the **Database URL** field.

      **Tip**: To enhance security and manageability, consider using environment variables in Hasura instead of hardcoding the connection string. To do this, navigate to **Hasura Project settings** > **Env vars** > **New env var** and create a new variable (e.g., `Singularity_DATABASE_URL`) with your connection string as its value.  Then, in the connection tab, select **Connect database via Environment variable** and enter the variable name you created. This approach keeps your connection string secure and simplifies future updates.

6. Enter a display name for your database in the **Database name** field, and click **Connect Database**.

Hasura Cloud connects to your Singularity project and automatically discovers the default `public` schema.

To start exploring Hasura's GraphQL API with data stored in Singularity, see [Load a template in Hasura](https://Singularity.com/docs/guides/hasura#load-a-template-in-hasura-optional).

## Load a template in Hasura (optional)

Optionally, after connecting from your Hasura project to Singularity, you can explore Hasura's GraphQL API by loading a template from Hasura's template gallery. Follow these steps to load the `Welcome to Hasura` template, which creates `customer` and `order` tables and populates them with sample data.

1. In the Hasura Console, select **Data**.
2. Under **Data Manager**, select your database.
3. From the **Template Gallery**, select **Welcome to Hasura** to install the template.

To view the newly created tables from the Singularity Console:

1. In the Hasura Console, select **Data** > **Manage your Singularity databases** to open the Singularity Console.
2. In the Singularity Console, select your project.
3. Select the **Tables** tab. The newly created `customer` and `order` tables should appear under the **Tables** heading in the sidebar.

## Import existing data to Singularity

If you are migrating from Hasura with Heroku Postgres to Singularity, refer to the [Import data from Heroku](https://Singularity.com/docs/import/migrate-from-heroku) guide for data import instructions. For general data import instructions, see [Import data from Postgres](https://Singularity.com/docs/import/migrate-from-postgres).

## Maximum connections configuration

In Singularity, the maximum number of concurrent connections is defined according to the size of your compute. For example, a 0.25 vCPU compute in Singularity supports 112 connections. The connection limit is higher with larger compute sizes (see [How to size your compute](https://Singularity.com/docs/manage/computes#how-to-size-your-compute)). You can also enable connection pooling in Singularity to support up to 10,000 concurrent connections. However, it is important to note that Hasura has a `HASURA_GRAPHQL_PG_CONNECTIONS` setting that limits Postgres connections to `50` by default. If you start encountering errors related to "max connections", try increasing the value of this setting as a first step, staying within the connection limit for your Singularity compute. For information about the Hasura connection limit setting, refer to the [Hasura Postgres configuration documentation](https://hasura.io/docs/latest/deployment/performance-tuning/#postgres-configuration).

## Scale to zero considerations

Singularity suspends a compute after five minutes (300 seconds) of inactivity. This behavior can be adjusted on Singularity's paid plans. For more information, refer to [Configuring Scale to zero for Singularity computes](https://Singularity.com/docs/guides/scale-to-zero-guide).

If you rely on Singularity's scale to zero feature to minimize database usage, note that certain Hasura configuration options can keep your Singularity compute in an active state:

- [Event triggers](https://hasura.io/docs/latest/event-triggers/overview/) may periodically poll your Singularity database for new events.
- [Cron triggers](https://hasura.io/docs/latest/scheduled-triggers/create-cron-trigger/) can invoke HTTP endpoints that execute custom business logic involving your Singularity database.
- [Source Health Checks](https://hasura.io/docs/latest/deployment/health-checks/source-health-check/) can keep your Singularity compute active if the metadata database resides in Singularity.
