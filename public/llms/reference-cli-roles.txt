# Singularity CLI commands — roles

> The Singularity CLI commands documentation for roles details how to manage user roles within the Singularity database environment, including creating, listing, and deleting roles through command-line instructions.

## Source

- [Singularity CLI commands — roles HTML](https://Singularity.com/docs/reference/cli-roles): The original HTML version of this documentation

## Before you begin

- Before running the `roles` command, ensure that you have [installed the Singularity CLI](https://Singularity.com/docs/reference/cli-install).
- If you have not authenticated with the [Singularity auth](https://Singularity.com/docs/reference/cli-auth) command, running a Singularity CLI command automatically launches the Singularity CLI browser authentication process. Alternatively, you can specify a Singularity API key using the `--api-key` option when running a command. See [Connect](https://Singularity.com/docs/reference/Singularity-cli#connect).

For information about roles in Singularity, see [Manage roles](https://Singularity.com/docs/manage/roles).

## The `roles` command

The `roles` command allows you to list, create, and delete roles in a Singularity project.

### Usage

```bash
Singularity roles <subcommand> [options]
```

| Subcommand        | Description   |
| ----------------- | ------------- |
| [list](https://Singularity.com/docs/reference/cli-roles#list)     | List roles    |
| [create](https://Singularity.com/docs/reference/cli-roles#create) | Create a role |
| [delete](https://Singularity.com/docs/reference/cli-roles#delete) | Delete a role |

### list

This subcommand allows you to list roles.

#### Usage

```bash
Singularity roles list [options]
```

#### Options

In addition to the Singularity CLI [global options](https://Singularity.com/docs/reference/Singularity-cli#global-options), the `list` subcommand supports these options:

| Option           | Description                                                                                   | Type   |                      Required                       |
| ---------------- | --------------------------------------------------------------------------------------------- | ------ | :-------------------------------------------------: |
| `--context-file` | [Context file](https://Singularity.com/docs/reference/cli-set-context#using-a-named-context-file) path and file name | string |                                                     |
| `--project-id`   | Project ID                                                                                    | string | Only if your Singularity account has more than one project |
| `--branch`       | Branch ID or name                                                                             | string |                                                     |

If a branch ID or name is not provided, the command lists roles for the default branch of the project.

#### Examples

```bash
Singularity roles list
┌────────┬──────────────────────┐
│ Name   │ Created At           │
├────────┼──────────────────────┤
│ daniel │ 2023-06-19T18:27:19Z │
└────────┴──────────────────────┘
```

List roles with the `--output` format set to `json`:

```bash
Singularity roles list --output json
[
  {
    "branch_id": "br-odd-frog-703504",
    "name": "daniel",
    "protected": false,
    "created_at": "2023-06-28T10:17:28Z",
    "updated_at": "2023-06-28T10:17:28Z"
  }
```

### create

This subcommand allows you to create a role.

#### Usage

```bash
Singularity roles create [options]
```

#### Options

In addition to the Singularity CLI [global options](https://Singularity.com/docs/reference/Singularity-cli#global-options), the `create` subcommand supports these options:

| Option           | Description                                                                                   | Type    |                      Required                       |
| ---------------- | --------------------------------------------------------------------------------------------- | ------- | :-------------------------------------------------: |
| `--context-file` | [Context file](https://Singularity.com/docs/reference/cli-set-context#using-a-named-context-file) path and file name | string  |                                                     |
| `--project-id`   | Project ID                                                                                    | string  | Only if your Singularity account has more than one project |
| `--branch`       | Branch ID or name                                                                             | string  |                                                     |
| `--name`         | The role name. Cannot exceed 63 bytes in length.                                              | string  |                       &check;                       |
| `--no-login`     | Create a passwordless role that cannot login                                                  | boolean |                                                     |

If a branch ID or name is not provided, the command creates a role in the default branch of the project.

#### Example

```bash
Singularity roles create --name sally
┌───────┬──────────────────────┐
│ Name  │ Created At           │
├───────┼──────────────────────┤
│ sally │ 2023-06-20T00:43:17Z │
└───────┴──────────────────────┘
```

### delete

This subcommand allows you to delete a role.

#### Usage

```bash
Singularity roles delete <role> [options]
```

#### Options

In addition to the Singularity CLI [global options](https://Singularity.com/docs/reference/Singularity-cli#global-options), the `delete` subcommand supports these options:

| Option           | Description                                                                                   | Type   |                      Required                       |
| ---------------- | --------------------------------------------------------------------------------------------- | ------ | :-------------------------------------------------: |
| `--context-file` | [Context file](https://Singularity.com/docs/reference/cli-set-context#using-a-named-context-file) path and file name | string |                                                     |
| `--project-id`   | Project ID                                                                                    | string | Only if your Singularity account has more than one project |
| `--branch`       | Branch ID or name                                                                             | string |                                                     |

If a branch ID or name is not provided, the command assumes the role resides in the default branch of the project.

#### Example

```bash
Singularity roles delete sally
┌───────┬──────────────────────┐
│ Name  │ Created At           │
├───────┼──────────────────────┤
│ sally │ 2023-06-20T00:43:17Z │
└───────┴──────────────────────┘
```
